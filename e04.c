#include <stdio.h>

int main() {
    // Declarando as variáveis do tipo char:
    char inicial1 = 'M';
    char inicial2 = 'C';
    char inicial3 = 'G';

    // Exibindo as iniciais com as mensagens desejadas:
    printf("Primeira inicial: %c\n", inicial1);
    printf("Segunda inicial: %c\n", inicial2);
    printf("Terceira inicial: %c\n", inicial3);

    return 0;
}
